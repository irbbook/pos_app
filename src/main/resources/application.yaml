quarkus:
  application:
      name: pos-app
  http:
    port: 8080
  datasource:
    db-kind: postgresql
    username: myuser
    password: mypassword
    jdbc:
      url: jdbc:postgresql://192.168.2.103:5432/POS_DB
  hibernate-orm:
    database:
      generation: update  # Use `none`, `update`, `validate`, or `drop-and-create`
    log:
      sql: true

